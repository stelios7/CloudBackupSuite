{
  "LatestVersion": "1.0.5",
  "Changelog": "feat(updater): force-close target app before update, fix manifest UTF-8 encoding, and ensure log directory is created

- Terminates running instance of main app to allow safe file replacement
- Writes manifest.json using UTF-8 with BOM to preserve non-ASCII characters
- Ensures 'logs' directory is created before writing logs
",
  "ZipFileName": "update_package.zip"
}
