<Window x:Class="Cloud_Backup_Core.Views.SettingsBackupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cloud_Backup_Core.Views"
        mc:Ignorable="d"
        Title="SettingsBackupView" Height="400" Width="500"
		Background="{StaticResource BackgroundMainBrush}"
		WindowStartupLocation="CenterScreen"
		>
	<Window.Resources>
		<Style TargetType="Button" BasedOn="{StaticResource PrimaryButton}" />
	</Window.Resources>
	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<!--Backup Folder Selection-->
		<TextBlock Text="Backup Folder:" FontWeight="Bold" />
		<StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,5,0,10">
			<TextBox Text="{Binding BackupFolder, UpdateSourceTrigger=PropertyChanged}" Width="350"/>
			<Button 
				Content="Browse..." Margin="5,0,0,0" 
				Style="{StaticResource MenuBarButton2}"
				Command="{Binding BrowseCommand}" />

		</StackPanel>

		<!--SQL Server Instance Name-->
		<StackPanel Orientation="Horizontal" Grid.Row="2">

			<TextBlock Text="SQL Server Instance:" FontWeight="Bold" Margin="0,5,5,5"/>
			<TextBox x:Name="txtSqlServer" Width="250" Margin="0,5,0,5"
				 Text="{Binding SqlServerName, UpdateSourceTrigger=PropertyChanged}"
				 HorizontalAlignment="Left"/>
		</StackPanel>
		<StackPanel Orientation="Horizontal" Grid.Row="3">
			<TextBlock Text="Username:" FontWeight="Bold" Margin="0,5,5,5"/>
			<TextBox x:Name="txtSqlUsername" Width="150" Margin="0,5,0,5"
				 Text="{Binding SqlUsername, UpdateSourceTrigger=PropertyChanged}"
				 HorizontalAlignment="Left"/>
			<TextBlock Text="Password:" FontWeight="Bold" Margin="10,5,5,5"/>
			<TextBox x:Name="txtSqlPassword" Width="150" Margin="0,5,0,5"
				 Text="{Binding SqlPassword, UpdateSourceTrigger=PropertyChanged}"
				 HorizontalAlignment="Left"/>
		</StackPanel>

		<!--Database Name Input-->
		<TextBlock Text="Database Name:" FontWeight="Bold" Grid.Row="4"/>
		<TextBox x:Name="txtDatabaseName" Grid.Row="5" Width="350" Margin="0,5,0,10"
				 Text="{Binding DatabaseName, UpdateSourceTrigger=PropertyChanged}"
				 HorizontalAlignment="Left"/>

		<!--Backup Now Button-->
		<StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Center">
			<Button Content="Backup Now" Height="35" Width="200"
					Command="{Binding BackupCommand}" />
		</StackPanel>

		<!--Backup Status-->

		<StackPanel Orientation="Horizontal" Grid.Row="7">
			<TextBlock x:Name="lblStatus" Text="Status: " Margin="0,10,0,0" FontWeight="Bold"/>
			<TextBlock Text="{Binding StatusMessage}" Grid.Row="5" Margin="0,10,0,0" FontWeight="Bold"/>
		</StackPanel>



		<!--Schedule Backup-->
		<StackPanel Grid.Row="8" Margin="0,10,0,0">
			<TextBlock Text="Schedule Backup (Daily at HH:mm):" FontWeight="Bold"/>
			<StackPanel Orientation="Horizontal">
				<TextBox x:Name="txtScheduleTime" Width="50" 
						 HorizontalContentAlignment="Center"
						 Text="{Binding ScheduledTime, UpdateSourceTrigger=PropertyChanged}"
						 />
				<Button Content="Set Schedule" Margin="10,0,0,0" 
						Command="{Binding SetScheduleCommand}"
						Style="{StaticResource MenuBarButton2}"/>
			</StackPanel>
		</StackPanel>

		<!-- Save Button -->
		<Button Content="Save Settings" Height="35" Width="200"
				Grid.Row="9"
					Command="{Binding SaveBackupSettingsCommand}" />
	</Grid>
</Window>
